SELECT ('A pessoa ' || nome || ' de CPF ' || ' possui o seguinte endereço: ' || endereco) AS texto FROM Colaboradores;

SELECT UPPER ('A pessoa ' || nome || ' de CPF ' || ' possui o seguinte endereço: ' || endereco) AS texto FROM Colaboradores;

SELECT LOWER ('A pessoa ' || nome || ' de CPF ' || ' possui o seguinte endereço: ' || endereco) AS texto FROM Colaboradores;